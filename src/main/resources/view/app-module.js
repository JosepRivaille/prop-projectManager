(function () {
    'use strict';

    /**
     * @ngdoc object
     * @name projectManager
     *
     * @description
     *
     */
    angular
        .module('projectManager', [
            'ui.router',
            'projectManager.documents'
        ])
        .run(function ($rootScope) {

        })
}());

